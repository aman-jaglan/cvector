/** Application-wide configuration constants. */

export const API_BASE_URL = "http://localhost:8000/api";

/** How often to poll the backend for fresh data (milliseconds) */
export const POLLING_INTERVAL_MS = 30_000;
